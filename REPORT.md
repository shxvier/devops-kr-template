\# Отчёт по контрольной работе DevOps: Git и ветвление



\- \*\*Студент:\*\* Журавлев Алексей

\- \*\*Основной репозиторий (GitHub):\*\* https://github.com/shxvier/devops-kr-template

\- \*\*Зеркало репозитория (GitLab):\*\* https://gitlab.com/shxvier/devops-kr-mirror

\- \*\*Pull Request:\*\* https://github.com/shxvier/devops-kr-template/pull/1



\---



\## Задание 1. Настройка репозиториев и зеркал

1\. Создан форк исходного репозитория преподавателя на GitHub.

2\. Создан пустой репозиторий-зеркало на GitLab.

3\. Локально подключён remote `upstream` преподавателя для отслеживания изменений.

4\. Для remote `origin` настроены два push-URL, что обеспечивает синхронную отправку коммитов одновременно в GitHub и GitLab.



\---



\## Задание 2. Разработка функционала («грязная» история)

1\. Создана отдельная рабочая ветка `feature/add-phone-validation`.

2\. Реализована функция `validate\_phone` в модуле `validator.py`.

3\. Добавлены модульные тесты для проверки корректности номеров в `test/test\_validator.py`.

4\. Создан черновой WIP-коммит с заготовкой для валидации ИНН.

5\. Зафиксирован коммит с исправлением опечатки в docstring функции.



\---



\## Задание 3. Интерактивный rebase

1\. Выполнена команда `git rebase -i HEAD\~4` для структурирования истории.

2\. Коммиты с тестами и исправлением опечатки объединены с основным коммитом реализации через команду `fixup`.

3\. Черновой коммит `wip: start INN validation` исключён из истории с помощью команды `drop`.

4\. В результате получен один атомарный коммит: `feat: add phone validation function`.



\---



\## Задание 4. Разрешение merge-конфликта

1\. Подтянуты свежие изменения преподавателя из ветки `upstream/feature/instructor-change`.

2\. Выполнена команда `git merge upstream/feature/instructor-change`, зафиксирован конфликт содержимого в `validator.py` и `test/test\_validator.py`.

3\. Конфликты разрешены вручную: объединены и сохранены функции валидации телефона (`validate\_phone`) и СНИЛС (`validate\_snils`), а также соответствующие наборы тестов.

4\. Создан коммит разрешения слияния.



\---



\## Задание 5. Pull Request и синхронизация зеркал

1\. Ветка отправлена в удалённый репозиторий на GitHub.

2\. Создан Pull Request в ветку `master` с подробным описанием изменений и чек-листом.

3\. Выполнено слияние методологией \*\*Squash and merge\*\*, ветка `feature/add-phone-validation` удалена.

4\. Локальная ветка `master` переключена и синхронизирована с `origin/master`.

5\. Выполнен `git push origin master`, благодаря чему итоговая линейная история коммитов продублировалась в зеркало на GitLab.

